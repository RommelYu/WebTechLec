Jan 31, 2018
HTTP Request Methods - Standard Methods

GET
>Used to request from the server the retrieval of the source identified by the request URI; the retrieved resource is returned in the
message body as an entity.
>Could make consolidated with restrictive or extent ask for headers should impact restrictive or fractional asset recovery. 
>Must be supported by all general-purpose servers.

HEAD
>Like GET but only data.
>Identical to GET, except the message body is not included in the response.
 
POST
>Used in submitting html form data.
>it can be submitted by a query.
>Request that the origin server accept the entity enclosed in the request as a new subordinate of the resource identified by the
Request-URI in the Request-Line.

PUT
>Create or replace the state of the target resource with the state.
>Define by the representation eclosed in the request message payload.
>Request the server to store the enclosed entity in the message under the specified request URI.

DELETE
>Request the server to delete the resource identified by the request URI.

OPTION 
>A to data over the correspondence choices accessible on the request/response chain distinguished by those Request-URI. 
>This method allows the client to determine the options and/or requirements associated with a resource, or the capabilities of a
server, without implying a resource action or initiating resource retrieval.

TRACE
>Echo back what the client recieve.
>Use for testing of the request chain.

CONNECT
>Reserved for use of tunneling proxy servers